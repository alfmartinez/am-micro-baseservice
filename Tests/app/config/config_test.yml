framework:
    trusted_hosts: ~
    secret: "test"
    translator:      { fallback: "fr" }
    test: ~
    templating:
        engines: ["twig"]
    router:
        resource: "%kernel.root_dir%/config/routing.yml"